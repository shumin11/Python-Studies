# Text type: str "Hello World"
# Numeric types: integer 20, float 20.5, complex 1j
# sequence type: list["apple", "banana", "cherry"], tuple ("apple", "banana", "cherry"),
# range(6)
# mapping type: dict {"name" : "Jon", "age" : 36}
# set type: set {"apple", "banada", "cherry"}, frozenset
# frozenset({"apple", "banana", "cherry"})
# boolean type: bool True/False
# binary types: bytes b"Hello", bytearray bytearray(5), memoryview memoryview(bytes(5))


#complex: numbers are written with a "j" as the imaginary part
x = 3+5j
y = 5j
z = -5j

#convert from int to float: a = float(x)
#convert from float to int: b = int(y)
#convert from int to complex: c = complex(x)

import random
print(random.randrange(1,10))

#int() - construct an integer number from an integer literal, a float literal(by romoving all decimals), or a string literal (providing the string represents a whole number)
#float() - construct a float number from an integer literal, a float literal or a string literal (providing the string represents a float or an integer)
#str() - construct a string from a wide variety of data types, including strings, integer literals and float literals






